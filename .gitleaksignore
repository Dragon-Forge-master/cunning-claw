# Synthetic fixtures the redaction and phone-line tests exist to catch are
# marked in place with a `gitleaks:allow` comment on their own line, so the
# allowance moves with the fixture. Line-number fingerprints here broke the
# scan on 25 Sept when new tests shifted the fixtures down a few lines.
# A real key would never live in a test file.
